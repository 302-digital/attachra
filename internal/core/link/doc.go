// Package link contains the domain logic for generating and resolving
// personal download links that replace stripped attachments. It must
// not depend on any adapter-specific code (e.g. Postfix milter) — see
// ADR-002.
package link
